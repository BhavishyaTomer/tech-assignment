import { HideDetailsPipe } from './hide-details.pipe';

describe('HideDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new HideDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
